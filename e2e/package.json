{
  "name": "securebuild-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for SecureBuild",
  "private": true,
  "scripts": {
    "test": "ts-node run-tests.ts",
    "test:headed": "ts-node run-tests.ts --headed",
    "test:ui": "ts-node run-tests.ts --ui",
    "test:debug": "ts-node run-tests.ts --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@testcontainers/postgresql": "^11.14.0",
    "@types/node": "^25.7.0",
    "testcontainers": "^11.10.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=24"
  }
}
